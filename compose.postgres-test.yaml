name: tandem-postgres-test

services:
  postgres:
    image: postgres:18.6-bookworm
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-tandem_test}
      POSTGRES_USER: ${POSTGRES_USER:-tandem_bootstrap}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set a disposable bootstrap password}
      POSTGRES_MIGRATION_USER: ${POSTGRES_MIGRATION_USER:-tandem_migrator}
      POSTGRES_MIGRATION_PASSWORD: ${POSTGRES_MIGRATION_PASSWORD:?Set a separate disposable migration password}
      POSTGRES_RUNTIME_USER: ${POSTGRES_RUNTIME_USER:-tandem_app}
      POSTGRES_RUNTIME_PASSWORD: ${POSTGRES_RUNTIME_PASSWORD:?Set a separate disposable runtime password}
      POSTGRES_RLS_OWNER_ROLE: ${POSTGRES_RLS_OWNER_ROLE:-tandem_rls_owner}
    ports:
      - "127.0.0.1:${POSTGRES_TEST_PORT:-55432}:5432"
    volumes:
      - postgres_test_data:/var/lib/postgresql
      - ./docker/postgres-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$$POSTGRES_USER\" -d \"$$POSTGRES_DB\""]
      interval: 3s
      timeout: 5s
      retries: 20

volumes:
  postgres_test_data:
